# 変更メモ（運用向け）

このファイルは、研究運用で「挙動の前提が変わった点」を短く残すためのメモ欄です。

## クリック割当の更新

連続タップ列の解釈を次に変更しました。

- 2回: 左シングル
- 3回: 左ダブル
- 4回以上: 右クリック

## 入力送信の更新

クリック/ドラッグの押下維持の安定性向上のため、左ボタンの押下・移動・解放に `pynput` を優先する実装へ寄せています（失敗時のみ `pyautogui` にフォールバック）。
